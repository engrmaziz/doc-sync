# Sample Project

Intro text with no relevant symbols.

## Authentication

Call `loginUser(username)` to log a user in. There is no attempt limit currently.

## Unrelated Section

This section talks about something else entirely, like `formatDate`.
